# graphify embedding config for benchmark fixture
# This enables sentence-transformers (local CPU, no API key) so the
# benchmark and test suite can run end-to-end without external services.
embed_backend=sentence-transformers
embed_model=paraphrase-multilingual-MiniLM-L12-v2
