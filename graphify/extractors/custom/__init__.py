"""Built-in custom extractors — auto-scanned by ``graphify.extractors.__init__``.

Drop a ``.py`` file here and it is imported at startup; if the module
registers itself via ``@register_doc_extractor``, it becomes active without
any manual import line. A single module failing to import only prints a
warning — it does not abort graphify startup.
"""
