# 假设草稿 — {系统名称}

> ⚠️ 临时文件（`docs/draft/`）。全程累积 ASSUME 决策，闭环删除。
> 规则：每次 ASSUME 决策**立即**记录到这里——"到最后"永远不会来。

---

## {BC 名称} — Step {N}

### A-001: {模糊点描述}
```
[ASSUMPTION: {模糊点描述}]
├─ 选择: {选定的选项}
├─ 备选: {被拒绝的选项 + 原因}
└─ 变更成本: LOW | MEDIUM
```
- **受影响产物**: {哪个产物文件}
- **记录于**: Step {N}
- **状态**: PENDING（等待用户评审）

---

## 汇总

| ID | BC | Step | 变更成本 | 状态 |
|----|----|------|:-------:|------|
| A-001 | {bc} | {step} | LOW/MEDIUM | PENDING |

## 评审记录（Step 9 闭环时填写）

| ID | 用户决定 | 需要返工？ | 返工产物 |
|----|---------|:---------:|---------|
| A-001 | 已确认 / 已纠正：{新值} | 是/否 | {清单} |

> 闭环删除：所有条目状态为"已确认"或"已纠正"后，连同整个 `docs/draft/` 目录删除。
