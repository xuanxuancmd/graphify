# 推断草稿 — {系统名称}

> ⚠️ 临时文件（`docs/draft/`）。从代码信号推出的候选业务语义，待用户确认后转为产物。闭环删除。
> 本文件含代码信号（含 file:line）和 `[INFERRED]` 标记——这些是中间态，不进入产物。

---

## 代码信号区

> 扫描到的代码信号：目录、类型、方法计数、命名模式。可含 file:line。
> 每次 Glob/Read/graphify 查询后立即摘要追加到此区。
> 标注使用的代码结构查询模式：graphify 模式 / 降级模式（Glob+Read+Grep）。

**代码结构查询模式**: {graphify 模式 / 降级模式}

### Step {N}: {扫描目标}

| 信号 | 位置 | 备注 |
|------|------|------|
| {类型/方法/模式} | {file:line} | {命名是否业务导向等} |

---

## 推断区

> 从代码信号推出的候选业务语义。每条推断内嵌其依据的代码信号。

### INF-001: {候选语义陈述}
```
[INFERRED: {候选业务语义}]
├─ 代码信号: {简述——引用上方信号区的条目}
├─ 候选理解: {用业务语言复述}
├─ 矛盾信号: {刻意找的反例——如有}
├─ 置信度: HIGH/MEDIUM/LOW
└─ 待问问题: {一次一问的问题}
```
- **记录于**: Step {N}
- **状态**: PENDING（待问用户）/ 已转为产物 / 已否决

---

## 汇总

| ID | Step | 置信度 | 状态 |
|----|------|:------:|------|
| INF-001 | {step} | HIGH/MEDIUM/LOW | PENDING |

> 状态流转：PENDING →（用户确认）→ 已转为产物 /（用户否决）→ 已否决
> 闭环前所有 INF 必须非 PENDING。闭环后连同整个 `docs/draft/` 目录删除。
