# 上下文图 — {系统名称}

## 1. 限界上下文

| BC ID | BC 名称 | 分类 | 代码根 | 职责（一句话业务定义） |
|-------|---------|------|--------|----------------------|
| BC-01 | {名称} | 核心域 | `{模块/包路径}` | {这个 BC 交付什么业务价值} |
| BC-02 | {名称} | 支撑域 | `{模块/包路径}` | {职责} |
| BC-03 | {名称} | 通用域 | `{模块/包路径}` | {职责} |

## 2. 业务关系

| 从 | 到 | 关系类型 | 同步/异步 | OHS 实现 | 业务含义 |
|------|----|---------|:---------:|---------|---------|
| BC-01 | BC-02 | 客户-供应商 | 同步 | — | {A 依赖 B 的什么业务承诺} |
| BC-01 | BC-03 | ACL | — | — | {A 通过防腐层隔离 B 的什么} |
| BC-01 | 外部 | OHS | 同步 | `{开放协议文件}` · `{URL 前缀}` | {A 对外暴露什么业务能力} |

### 2.1 对外契约文件清单

| BC | 契约类型 | 协议 | 文件路径 |
|----|---------|------|---------|
| BC-01 | openapi | HTTP | `{file_path}` |

## 3. 统一语言

| 术语 | 定义 | 跨 BC 歧义？ |
|------|------|:-----------:|
| {术语} | {业务定义} | {标记歧义或无} |

## 4. 领域愿景声明

{1 段话：这个系统交付的核心业务价值是什么，哪个 BC 是核心交付者}
