# 聚合协作视图 — {BC 名称}

## 1. 聚合根

| ID | 聚合根<anchor:ddd> | 代码锚点<anchor:code> | 持久化表 | 业务职责（一句话）<anchor:desc> | 业务入口概念？ |
|----|--------|---------|---------|------------------|:------------:|
| AG-01 | {业务术语名称} | `{ClassName}` | `{table_name}` | {管理什么业务概念} | 是/否 |

## 2. 领域实体

| ID | 领域实体<anchor:ddd> | 代码锚点<anchor:code> | 归属聚合 | 业务标识 | 业务职责<anchor:desc> |
|----|------|--------|---------|----------|---------|
| EN-01 | {业务术语名称} | `{ClassName}` | AG-01 | {标识业务含义} | {业务上代表什么} |

## 3. 值对象

| ID | 值对象<anchor:ddd> | 代码锚点<anchor:code> | 归属聚合 | 业务含义<anchor:desc> |
|----|--------|--------|---------|---------|
| VO-01 | {业务术语名称} | `{ClassName}` | AG-01 | {描述什么业务属性} |

## 4. 聚合协作视图

| 聚合协作<anchor:ddd> | 源聚合 | 协作 | 目标聚合 | 代码锚点<anchor:code> | 业务性质<anchor:desc> | 业务触发条件 |
|----|------|----|---------|---------|---------|-------------|
| {描述性协作名称} | {聚合业务术语} | 依赖/管理/使用 | {聚合业务术语或BC} | `{ClassName}.{method}` | {业务依赖性质} | {触发条件} |

```mermaid
graph LR
    {AG01_NAME} -->|"{业务依赖性质}"| {AG02_NAME}
    {AG02_NAME} -.->|"{状态依赖: 业务条件}"| {AG01_NAME}
```

> 协作视图的代码锚点填发起协作的方法（`类名.函数名`）。若是调用链，填入口方法，在"业务性质"中补充"调用链：A → B → C"。

## 5. 状态机（可选）

### SM-01: {实体/聚合名} 状态机

**归属**: {AG/EN ID} · **状态枚举**: `{EnumClassName}`

| 状态转换<anchor:ddd> | 当前状态 | 业务触发 | 目标状态 | 代码锚点<anchor:code> | 守卫条件（业务含义）<anchor:desc> |
|----------|----------|---------|---------|---------|---------------------|
| {源状态→目标状态} | {当前状态} | {触发动作} | {目标状态} | `{ClassName}.{method}` | {业务上必须满足什么} |

```mermaid
stateDiagram-v2
    [*] --> {状态A}
    {状态A} --> {状态B} : {业务触发}
    {状态B} --> [*]
```

> 状态机的代码锚点填执行该状态转换的方法（`类名.函数名`）。同一方法处理多个转换时重复填写。

## 6. 行为归属与领域服务（可选）

**本 BC 的模型风格**: 充血 / 贫血 / 混合

| 聚合/实体 | 代码锚点 | 行为位置 | 说明 |
|-----------|---------|---------|------|
| {聚合/实体业务术语} | `{ClassName}` | 实体上（充血）/ 外部 Service（贫血） | {说明} |

### 领域服务（可选，贫血模型才展开）

| ID | 领域服务<anchor:ddd> | 代码锚点<anchor:code> | 操作的聚合 | 业务行为<anchor:desc> | 业务规则 |
|----|--------|--------|-----------|---------|---------|
| DS-01 | {业务术语名称} | `{ClassName}` | AG-01 | {承载什么业务行为} | {含什么业务规则} |
