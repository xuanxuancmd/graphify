# 业务不变式 — {BC 名称}

## 不变式目录

| ID | 业务不变式<anchor:ddd> | 代码锚点<anchor:code> | 业务理由<anchor:desc> | 违反后果 |
|----|--------|---------|---------|---------|
| INV-001 | {规则陈述} | `{ClassName}.{method}` | {为什么必须为真；跨 BC 注明保证方；不知标 UNKNOWN} | {业务上意味着什么} |
| INV-002 | {规则陈述} | `{ClassName}.{method}` | {理由} | {后果} |

> 代码锚点列填 enforce 该不变式的函数（`类名.函数名` 格式）。若不变式由构造函数或类型系统保证，填 `类名`。无法定位时 STOP 向用户核实。
