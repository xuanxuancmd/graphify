# DDD Delta — 聚合协作视图 — {BC 名称}

> 模式二（`--changes`）产物。记录本次变更新增/修改/删除的聚合根/实体/值对象/协作/状态机/领域服务。
> 格式规范见 [references/incremental-merge.md](../../references/incremental-merge.md)。
> 每个 section 独立合并——无变更的 section 不出现在 delta 中。

## ADDED

### 聚合根

| ID | 聚合根<anchor:ddd> | 代码锚点<anchor:code> | 持久化表 | 业务职责（一句话）<anchor:desc> | 业务入口概念？ |
|----|--------|---------|---------|------------------|:------------:|
| AG-{NN} | {业务术语名称} | `{ClassName}` | `{table_name}` | {管理什么业务概念} | 是/否 |

### 领域实体

| ID | 领域实体<anchor:ddd> | 代码锚点<anchor:code> | 归属聚合 | 业务标识 | 业务职责<anchor:desc> |
|----|------|--------|---------|----------|---------|
| EN-{NN} | {业务术语名称} | `{ClassName}` | AG-{NN} | {标识业务含义} | {业务上代表什么} |

### 值对象

| ID | 值对象<anchor:ddd> | 代码锚点<anchor:code> | 归属聚合 | 业务含义<anchor:desc> |
|----|--------|--------|---------|---------|
| VO-{NN} | {业务术语名称} | `{ClassName}` | AG-{NN} | {描述什么业务属性} |

### 聚合协作视图

| 聚合协作<anchor:ddd> | 源聚合 | 协作 | 目标聚合 | 代码锚点<anchor:code> | 业务性质<anchor:desc> | 业务触发条件 |
|----|------|----|---------|---------|---------|-------------|
| {描述性协作名称} | {聚合业务术语} | 依赖/管理/使用 | {聚合业务术语或BC} | `{ClassName}.{method}` | {业务依赖性质} | {触发条件} |

### 状态机

| 状态转换<anchor:ddd> | 当前状态 | 业务触发 | 目标状态 | 代码锚点<anchor:code> | 守卫条件（业务含义）<anchor:desc> |
|----------|----------|---------|---------|---------|---------------------|
| {源状态→目标状态} | {当前状态} | {触发动作} | {目标状态} | `{ClassName}.{method}` | {业务上必须满足什么} |

### 领域服务

| ID | 领域服务<anchor:ddd> | 代码锚点<anchor:code> | 操作的聚合 | 业务行为<anchor:desc> | 业务规则 |
|----|--------|--------|-----------|---------|---------|
| DS-{NN} | {业务术语名称} | `{ClassName}` | AG-{NN} | {承载什么业务行为} | {含什么业务规则} |

## MODIFIED

### 聚合根

| ID | 聚合根<anchor:ddd> | 代码锚点<anchor:code> | 持久化表 | 业务职责（一句话）<anchor:desc> | 业务入口概念？ |
|----|--------|---------|---------|------------------|:------------:|
| AG-{NN} | {修改后的业务术语} | `{ClassName}` | `{table_name}` | {修改后的业务职责} | 是/否 |

### 领域实体

| ID | 领域实体<anchor:ddd> | 代码锚点<anchor:code> | 归属聚合 | 业务标识 | 业务职责<anchor:desc> |
|----|------|--------|---------|----------|---------|
| EN-{NN} | {修改后的业务术语} | `{ClassName}` | AG-{NN} | {修改后的标识} | {修改后的职责} |

### 值对象

| ID | 值对象<anchor:ddd> | 代码锚点<anchor:code> | 归属聚合 | 业务含义<anchor:desc> |
|----|--------|--------|---------|---------|
| VO-{NN} | {修改后的业务术语} | `{ClassName}` | AG-{NN} | {修改后的含义} |

### 聚合协作视图

| 聚合协作<anchor:ddd> | 源聚合 | 协作 | 目标聚合 | 代码锚点<anchor:code> | 业务性质<anchor:desc> | 业务触发条件 |
|----|------|----|---------|---------|---------|-------------|
| {修改后的协作名称} | {源聚合} | {协作} | {目标聚合} | `{ClassName}.{method}` | {修改后的业务性质} | {修改后的触发条件} |

### 状态机

| 状态转换<anchor:ddd> | 当前状态 | 业务触发 | 目标状态 | 代码锚点<anchor:code> | 守卫条件（业务含义）<anchor:desc> |
|----------|----------|---------|---------|---------|---------------------|
| {源状态→目标状态} | {当前状态} | {修改后的触发} | {目标状态} | `{ClassName}.{method}` | {修改后的守卫条件} |

### 领域服务

| ID | 领域服务<anchor:ddd> | 代码锚点<anchor:code> | 操作的聚合 | 业务行为<anchor:desc> | 业务规则 |
|----|--------|--------|-----------|---------|---------|
| DS-{NN} | {修改后的业务术语} | `{ClassName}` | AG-{NN} | {修改后的业务行为} | {修改后的业务规则} |

## REMOVED

### 聚合根

| ID |
|----|
| AG-{NN} |

### 领域实体

| ID |
|----|
| EN-{NN} |

### 值对象

| ID |
|----|
| VO-{NN} |

### 聚合协作视图

| 聚合协作 |
|---------|
| {描述性协作名称} |

### 状态机

| 状态转换 |
|----------|
| {源状态→目标状态} |

### 领域服务

| ID |
|----|
| DS-{NN} |

## RENAMED

### 聚合根

| ID | 旧术语 | 新术语 |
|----|--------|--------|
| AG-{NN} | {旧业务术语} | {新业务术语} |

### 领域实体

| ID | 旧术语 | 新术语 |
|----|--------|--------|
| EN-{NN} | {旧业务术语} | {新业务术语} |

### 值对象

| ID | 旧术语 | 新术语 |
|----|--------|--------|
| VO-{NN} | {旧业务术语} | {新业务术语} |

### 领域服务

| ID | 旧术语 | 新术语 |
|----|--------|--------|
| DS-{NN} | {旧业务术语} | {新业务术语} |
