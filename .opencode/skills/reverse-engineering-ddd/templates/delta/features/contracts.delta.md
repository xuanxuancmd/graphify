# DDD Delta — 契约 — {BC 名称}

> 模式二（`--changes`）产物。记录本次变更新增/修改/删除的契约。
> 格式规范见 [references/incremental-merge.md](../../references/incremental-merge.md)。

## ADDED

### 跨 BC 契约

| 跨BC契约<anchor:ddd> | 契约 ID | 方向 | 对端 BC | 同步/异步 | 协议 | 代码锚点<anchor:code> | operationId | 业务承诺<anchor:desc> | 失败时业务后果 |
|---------|------|---------|:---------:|------|---------|-------------|---------|--------------|
| {描述性契约名称} | C-{xx}-{yy} | 入站/出站 | {BC 名称} | 同步/异步 | {协议} | `{锚点}` | {operationId} | {业务承诺} | {失败后果} |

### BC 内契约

### C-{NNN}: {操作/承诺名称}
- **接口**: `{TraitName}` 或 `{ServiceName}.{method}`
- **业务承诺**: {这个契约在业务上承诺什么}
- **前置条件**: {调用前必须满足的业务状态}
- **后置条件**: {调用后承诺的业务状态}
- **失败语义**: {失败时业务上留下什么状态}
- **幂等性**: {重复调用在业务上是否安全/等效}

## MODIFIED

### 跨 BC 契约

| 跨BC契约<anchor:ddd> | 契约 ID | 方向 | 对端 BC | 同步/异步 | 协议 | 代码锚点<anchor:code> | operationId | 业务承诺<anchor:desc> | 失败时业务后果 |
|---------|------|---------|:---------:|------|---------|-------------|---------|--------------|
| {修改后的契约名称} | C-{xx}-{yy} | {方向} | {BC 名称} | {同步/异步} | {协议} | `{锚点}` | {operationId} | {修改后的业务承诺} | {修改后的失败后果} |

### BC 内契约

### C-{NNN}: {修改后的操作/承诺名称}
- **接口**: `{TraitName}` 或 `{ServiceName}.{method}`
- **业务承诺**: {修改后的业务承诺}
- **前置条件**: {修改后的前置条件}
- **后置条件**: {修改后的后置条件}
- **失败语义**: {修改后的失败语义}
- **幂等性**: {修改后的幂等性}

## REMOVED

### 跨 BC 契约

| 契约 ID |
|---------|
| C-{xx}-{yy} |

### BC 内契约

| 契约 ID |
|---------|
| C-{NNN} |

## RENAMED

### 跨 BC 契约

| 契约 ID | 旧术语 | 新术语 |
|---------|--------|--------|
| C-{xx}-{yy} | {旧描述性名称} | {新描述性名称} |
