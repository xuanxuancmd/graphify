# DDD Delta — 业务流程 — {BC 名称}

> 模式二（`--changes`）产物。记录本次变更新增/修改/删除的业务用例（时序编排 + 失败补偿）。
> 格式规范见 [references/incremental-merge.md](../../references/incremental-merge.md)。

## ADDED

### 用例: {用例名称}

**用例 ID**: UC-{xx}-{yy}

#### 入口点

| 用例入口<anchor:ddd> | 入口类型 | 代码锚点<anchor:code> | 业务触发<anchor:desc> |
|---------|---------|---------|---------|
| {唯一业务名称} | 应用服务/命令处理器/跨BC入站端口/周期任务/外部触发 | `{ServiceName}.{method}` | {什么业务场景触发} |

#### 时序编排

| 步骤 | 业务动作<anchor:ddd> | 代码锚点<anchor:code> | 核心角色 / BC | 业务理由（WHY）<anchor:desc> |
|------|---------|---------|-------------|--------------|
| 1 | {业务动作} | `{ClassName}.{method}` | {本 BC / 对端 BC 名} | {为什么这一步存在} |

#### 失败/补偿矩阵

| 失败点<anchor:ddd> | 代码锚点<anchor:code> | 失败的业务后果 | 补偿动作 | 恢复原状态？ | 业务理由<anchor:desc> |
|--------|---------|--------------|---------|:-----------:|---------|
| {描述性失败名称} | `{ClassName}.{method}` | {业务上留下什么状态} | {业务上如何补偿} | 是/否 | {为什么这样补偿} |

## MODIFIED

### 用例: {用例名称}

**用例 ID**: UC-{xx}-{yy}

> MODIFIED 用例必须携带完整的入口点/时序编排/失败补偿表（含未变更的行），防止"半截修改"丢步骤。

#### 入口点

| 用例入口<anchor:ddd> | 入口类型 | 代码锚点<anchor:code> | 业务触发<anchor:desc> |
|---------|---------|---------|---------|
| {完整行——含未变更的入口点} | | | |

#### 时序编排

| 步骤 | 业务动作<anchor:ddd> | 代码锚点<anchor:code> | 核心角色 / BC | 业务理由（WHY）<anchor:desc> |
|------|---------|---------|-------------|--------------|
| 1 | {完整行——含未变更的步骤} | | | |
| 2 | {修改后的步骤} | `{ClassName}.{method}` | {角色} | {修改后的理由} |

#### 失败/补偿矩阵

| 失败点<anchor:ddd> | 代码锚点<anchor:code> | 失败的业务后果 | 补偿动作 | 恢复原状态？ | 业务理由<anchor:desc> |
|--------|---------|--------------|---------|:-----------:|---------|
| {完整行——含未变更的失败点} | | | | | |

## REMOVED

### 用例

| 用例 ID |
|---------|
| UC-{xx}-{yy} |

## RENAMED

### 用例入口

| 用例 ID | 旧术语 | 新术语 |
|---------|--------|--------|
| UC-{xx}-{yy} | {旧入口业务名称} | {新入口业务名称} |

### 业务动作

| 用例 ID | 步骤 | 旧术语 | 新术语 |
|---------|------|--------|--------|
| UC-{xx}-{yy} | {步骤号} | {旧业务动作名} | {新业务动作名} |
