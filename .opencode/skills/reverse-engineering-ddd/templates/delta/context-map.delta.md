# DDD Delta — 上下文图 — {系统名称}

> 模式二（`--changes`）产物。记录本次变更新增/修改/删除的 BC 边界、关系、统一语言。
> 格式规范见 [references/incremental-merge.md](../../references/incremental-merge.md)。

## ADDED

### 限界上下文

| BC ID | BC 名称 | 分类 | 代码根 | 职责（一句话业务定义） |
|-------|---------|------|--------|----------------------|
| BC-{NN} | {名称} | 核心域/支撑域/通用域 | `{模块/包路径}` | {这个 BC 交付什么业务价值} |

### 业务关系

| 从 | 到 | 关系类型 | 同步/异步 | OHS 实现 | 业务含义 |
|------|----|---------|:---------:|---------|---------|
| BC-{XX} | BC-{YY} | {关系类型} | {同步/异步} | — | {A 依赖 B 的什么业务承诺} |

### 统一语言

| 术语 | 定义 | 跨 BC 歧义？ |
|------|------|:-----------:|
| {术语} | {业务定义} | {标记歧义或无} |

## MODIFIED

### 限界上下文

| BC ID | BC 名称 | 分类 | 代码根 | 职责（一句话业务定义） |
|-------|---------|------|--------|----------------------|
| BC-{NN} | {修改后的名称} | {分类} | `{代码根}` | {修改后的职责} |

### 业务关系

| 从 | 到 | 关系类型 | 同步/异步 | OHS 实现 | 业务含义 |
|------|----|---------|:---------:|---------|---------|
| BC-{XX} | BC-{YY} | {修改后的关系类型} | {同步/异步} | — | {修改后的业务含义} |

### 统一语言

| 术语 | 定义 | 跨 BC 歧义？ |
|------|------|:-----------:|
| {术语} | {修改后的定义} | {标记歧义或无} |

### 领域愿景声明

{修改后的完整段落——整体替换 baseline 中的领域愿景声明}

## REMOVED

### 限界上下文

| BC ID |
|-------|
| BC-{NN} |

### 业务关系

| 从 | 到 |
|------|----|
| BC-{XX} | BC-{YY} |

### 统一语言

| 术语 |
|------|
| {术语} |

## RENAMED

### 统一语言

| 术语（旧） | 术语（新） |
|------------|------------|
| {旧术语} | {新术语} |
